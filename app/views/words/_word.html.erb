<div class="col-sm-12 col-lg-4 mb-3">
    <div id="word-id-<%= word.id %>">
          <h4 class="card-title">
          <%= link_to word.title, words_path(word) %>
          </h4>
            <div class='ms-auto'>
              <%= link_to edit_word_path(word), id: "button-edit-#{word.id}" do %>
                <i class="bi bi-pencil-fill"></i>
              <% end %>
              <%= link_to words_path(word), id: "button-delete-#{word.id}", data: { turbo_method: :delete, turbo_confirm: t('defaults.delete_confirm') } do %>
                <i class="bi bi-trash-fill"></i>
              <% end %>
            </div>
          <ul class="list-inline">
        </ul>
    </div>
</div>
